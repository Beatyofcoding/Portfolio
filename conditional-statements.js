let age = 66;

let response;

if (age <0) {
    response = 'You weren\'t even born!';
}   else if (age < 18) {
    response = 'Too young'
} else if (age>65) {
    response = 'Too old';
} else {
    response = 'Just the right age'
}

let response = age < 0
? 'You weren\'t even born!'
: age <18
 ?'Too young'
 : age > 65
  ? 'Too old'
  : 'Just the right age';














if (age >= 18) {
    isAdult = true;
}   else {
    isAdult = false;
} 

let isYoung






let age = 40

let result1 = (age < 0 || (age > 65 && age =0);
let result2 = (age > 18 && age < 65) || age = 0;



let result = 5+2*3

if (age<0 !! (age > 65 && age = 0))
